# 发布前完整性审查（2026-09-07 BI）

> 版本状态（2026-09-08）：当前正式版本为 **1.0.0**，以[1.0.0 发布范围](RELEASE-SCOPE-1.0.0.md)和[发布说明](RELEASE-NOTES-1.0.0.md)为准。下文保留 **0.1.0a1 阶段的历史快照**；其中“当前”“预览”“待发布”、测试数量和工件 SHA256 均对应当时，不代表 1.0.0 的构建或发布审核结果。原广泛验收的缺项和真实登记状态保留，不改写为通过。

源码及 BI 安装版、便携版均开放小米、OPPO、vivo、华为备忘录、荣耀、魅族、WPS 七个受限开发预览迁入目标。PNG/JPEG 原件、账号隔离、跨版本未知回执保护继续强制生效；小米限已核实非加密账号及单张 4 MiB。OPPO 大文件分片未完成实测，不宣称全面支持。 版本保持 `0.1.0a1`，`release_ready=false`。正式迁移登记仍为 0/42；用户确认与固定样例通过不等于正式 42 方向及桌面环境全部通过。 未公开推送或 Release。

## 已核对与边界

| 项目 | 已核对结果 | 边界 |
| --- | --- | --- |
| 非 OPPO 30 方向 | 用户亲自确认正常，自动证据分别保留 | 冻结，不重复要求登录；不是正式全环境通过 |
| OPPO 12 方向 | 24 条固定直接来源样例的目标及源端后置；11 个原 API 通过及 BI4 独立恢复 | BI4 原 22 条正文未重核，原失败不改写 |
| OPPO 官网 | OR1＋六迁入双条，13 条／24 图精确核对通过 | 12 条限定标记修复、BI5[0] 未改；代码视觉降级明确 |
| 其他目标显示 | WPS／华为／荣耀六条十二图官网通过；小米两条四图已知视觉通过；vivo 用户正常 | 小米不称完整排版；魅族无表格边框为预定普通文字降级 |
| 导出 | 56 项历史基础转换检查及后来合成渲染回归 | 不是全部历史内容完整验收；不重读 vivo 私人笔记 |
| BI 运行包 | 1685 项完整测试，本机安装 9/9，冻结 EXE 登录隔离与清理通过 | 合成登录测试不访问厂商；本机不代替干净系统 |

OPPO 已按官网规则使用 `<i>` 斜体、带内联背景色的 `highlight_color_yellow` 高亮和 `hr-style-solid` 分隔线；下划线按官网渐变线与对应模型标记核对。代码块与行内代码在官网显示为普通文字，差异明确报告，原始代码文字及标记保留供本地导出。

源端后置“未变”只在各原始证明的观察时点及固定范围成立。此后 OR1 与部分既有 OPPO 目标进行了有独立授权和证据的格式标记修复，版本随之变化；不能把旧来源指纹／版本证明写成当前永久未变。修复只更新既有样例，不新增笔记，旧失败、原始源端证明和修复证明分别保留。

## 工件、历史与审批

BI 七目标开发预览已通过 1685 项完整测试、前端构建、PyInstaller/NSIS；便携包 2,349 个文件逐一校验一致，本机 Windows 11 中文路径安装、升级、卸载保留导出等 9/9 项通过。新冻结 EXE 的独立登录子进程、会话隔离及临时目录清理已用本地合成页面验证；该检查不访问厂商或个人资料，也不代表厂商会话永久有效。

| 工件 | SHA256 |
| --- | --- |
| 安装包 | `c51911f366d9be980dc95028112947253773cdf3456cca1483caee7aaf70d5bb` |
| 便携 ZIP | `5dcadc6158e16e06584d606135830ace20ea20817e82eedc7e57e87fcb4131f9` |

源码 ZIP 在最终说明同步后由独立步骤刷新，最终校验值以交付目录 `SHA256SUMS.txt` 和 `build-manifest.json` 为准。源码包包含本说明，故不在包内嵌入其自身最终 SHA256。

历史 BF 是六目标版本，完整测试 1160 项、本机安装 9/9；BH 仅同步了当时登录工具、测试和说明。两者的原证据和校验值保留，不能代替本轮 BI 的实际构建及验收。

干净 Windows 10/11、各缩放与全部界面状态、手机同步、完整空白排版及未覆盖内容／失败场景仍有缺项。本机检查不代替这些条件；已声明降级、历史未知写入和失败证据保留，不自动重发或清除保护。

正式登记仍为 56 项导出、42 项迁移、6 项桌面环境、8 项界面状态，共 112 项，状态不因本次文档同步而改变。这不是 112 项全部运行失败。详见 [迁移矩阵](MIGRATION-MATRIX.md)、[兼容性说明](COMPATIBILITY.md) 和 [交付审阅](REVIEW-PACKAGE.md)。源码最终说明同步后独立刷新并校验，再由用户审批明确版本与发布范围；不得称正式 v1.0.0 全部通过。
