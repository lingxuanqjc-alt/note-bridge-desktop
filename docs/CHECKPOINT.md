> 版本状态（2026-09-08）：当前正式版本为 **1.0.0**，以[1.0.0 发布范围](RELEASE-SCOPE-1.0.0.md)和[发布说明](RELEASE-NOTES-1.0.0.md)为准。下文保留 **0.1.0a1 阶段的历史快照**；其中“当前”“预览”“待发布”、测试数量和工件 SHA256 均对应当时，不代表 1.0.0 的构建或发布审核结果。原广泛验收的缺项和真实登记状态保留，不改写为通过。

2026-09-07 BI 最终本机交付核对：源码及 BI 安装版、便携版均开放小米、OPPO、vivo、华为备忘录、荣耀、魅族、WPS 七个受限开发预览迁入目标。PNG/JPEG 原件、账号隔离、跨版本未知回执保护继续强制生效；小米限已核实非加密账号及单张 4 MiB。OPPO 大文件分片未完成实测，不宣称全面支持。

BI 七目标开发预览已通过 1685 项完整测试、前端构建、PyInstaller/NSIS；便携包 2,349 个文件逐一校验一致，本机 Windows 11 中文路径安装、升级、卸载保留导出等 9/9 项通过。新冻结 EXE 的独立登录子进程、会话隔离及临时目录清理已用本地合成页面验证；该检查不访问厂商或个人资料，也不代表厂商会话永久有效。

OPPO 相关 12 方向、24 条固定样例已完成目标及源端后置；非 OPPO 30 方向由用户确认后冻结。OPPO 原始综合样例 OR1 与六个迁入双条，共 13 条、24 张图片，已完成精确身份、版本、分组、完整非空白正文顺序、目标保留样式、结构及图片解码的官网核对。12 条受影响样例已作限定格式修复，BI5[0] 无需修改；代码显示降级单列，不算代码布局原样保留。

源端后置“未变”只在各原始证明的观察时点及固定范围成立。此后 OR1 与部分既有 OPPO 目标进行了有独立授权和证据的格式标记修复，版本随之变化；不能把旧来源指纹／版本证明写成当前永久未变。修复只更新既有样例，不新增笔记，旧失败、原始源端证明和修复证明分别保留。

华为 BI4 原 22 条正文未重新核验；旧失败与未知回执仍保留。版本保持 `0.1.0a1`，`release_ready=false`。正式迁移登记仍为 0/42；用户确认与固定样例通过不等于正式 42 方向及桌面环境全部通过。

| 工件 | SHA256 |
| --- | --- |
| 安装包 | `c51911f366d9be980dc95028112947253773cdf3456cca1483caee7aaf70d5bb` |
| 便携 ZIP | `5dcadc6158e16e06584d606135830ace20ea20817e82eedc7e57e87fcb4131f9` |

源码 ZIP 在最终说明同步后由独立步骤刷新，最终校验值以交付目录 `SHA256SUMS.txt` 和 `build-manifest.json` 为准。源码包包含本说明，故不在包内嵌入其自身最终 SHA256。

以下 BH、BG、BF 及更早段落保留各时点事实，旧暂停、待验和“源码与二进制一致”不能用于描述当前工作区。

2026-09-07 BH 源码交付检查点：普通 Edge 可登录并打开 OPPO 正文（用户确认），单平台独立 Edge 测试窗口仍提示环境风险（用户确认，已关闭）。用户随后确认独立 WebView2 的“笔记互迁 · OPPO 测试登录”已正常进入，既有测试后端实际账号检查也已通过。普通 Edge 配置与凭据未导入。仅 OPPO 启动范围通过 4 项 Node 离线测试；WebView2 入口通过 18 项离线测试、Ruff 和独立静态审查。本次仅更新 BH 登录工具、测试和对应说明，生产代码与已验证的 BF 安装包、便携包保持不变。账号检查通过不代表 OR1 综合种子或 OPPO 的 12 个方向验收通过，产品 OPPO 迁入仍关闭；后续实际任务结果独立留档。其余 30 个方向保持用户验收完成，不重复请求登录。完整七平台正式发布尚未通过。

2026-09-07 BG 退出前检查点：用户亲自确认非 OPPO 的 30 个方向均验收正常，停止非 OPPO 的进一步实测与重新登录请求。所有实验指令未启用，OR1 未启用，没有待执行写入；旧未知回执继续隔离。OPPO 仍提示环境风险，下一步由用户通过 [官方登录入口说明](OPPO-LOGIN.md) 确认风险解除，再恢复实际测试会话。

BG 验收工具收尾：WPS／荣耀迁入华为的新四条官网精确身份、分组、独立标题、完整非空白正文、目标保留样式及八张图片通过；历史七条仅完成本地严格绑定，按用户验收停止进一步官网检查。37 项华为定点测试、8 项荣耀 Node 测试、Ruff 通过。生产程序未变，复用 BF 的 1160 项完整测试及本机 9 项安装证据，仅同步源码与文档并重新校验。

恢复先读 `.private/implementation-checkpoint.json` 和 `.private/evidence/user-non-oppo-acceptance-BG.json`。Codex 退出后原终端编号、进程和内存会话不得假定存续；没有保存凭据。未执行公开推送或 Release。

以下 BF 及更早记录为对应批次的历史事实。

2026-09-07 BF 六目标开发预览：1160 项完整测试、前端构建、PyInstaller/NSIS 通过；便携 ZIP 2,349 个文件逐一校验一致。本机 Windows 11 中文路径安装、资源、WebView2 桥接、升级、卸载及保留导出等 9 项检查通过。 安装包 SHA256：`47037a07a68360cafadd41fd49c17e26cd492a667887b2e72455c48dca413c62`。华为迁入及图片下载修复已进入本包，OPPO 仍关闭。首次构建的临时文件权限错误、官网超时与旧 BE 证据均保留；完整七平台正式验收未完成。

最新云端进展：荣耀→小米 BD7 两条、魅族→小米 BD8 三条的官网检查已精确绑定当前编辑器与确认回执 ID，五条已知视觉项目通过；魅族共享源端 28 条及 36 处附件引用不变。魅族→vivo BD11 三条、四张图片在工具样例范围内严格回读通过，目标指定范围由 20 条增至 23 条，原指定样例不变；只核对本轮所选魅族来源，不声称 vivo 全账号完整。WPS→魅族 BD12 两条、四张图片严格回读通过，目标原有 28 条不变，WPS 源端 29 条、42 处附件引用复读不变。官网自动核对在分组定位超时，按用户冻结范围保留缺口，不补写通过。

跨平台并行入口已通过专项检查及真实调度，同平台独占，产品仍一次执行一个数据任务。vivo BD11 三条工具样例的独立捕获和 8 项 ZIP 搬移导出通过，不读取私人笔记。华为 K2＋BD2 固定来源已完成五个出向 BD13–BD17，各两条、四原件的接口及共享源端后置核对通过；小米/WPS 官网已知项目通过，荣耀官网分组定位超时、魅族账号阶段过期、vivo 精确官网路径仍未验。

华为按笔记复用图片下载准备已通过真实 18、19、21、22 条完整读取，原有内容及每批唯一新增均有独立证据。本轮生产改动仅涉及三个 provider 文件，均已包含在 BF 包中。会话长时间稳定性仍未证明。

最新华为迁入：BD18 首条恢复通过，BD19 两条由独立 21 条读取确认原有 19 条不变、正文/分组/四原件通过；BD20 只补 BD18 未创建的第二条，迁入后真实 22 条读取确认原 21 条不变且唯一新增。荣耀源端 28 条/34 处引用、WPS 31 条/46 处引用复读不变。原 BD18/19 失败文件保持，未重启旧任务。BD15 华为→荣耀完成两条/四原件，原有 28 条不变；五出向只共享一次两条/四原件的源端核对并通过。

用户最新决定：冻结小米、vivo、WPS、荣耀、魅族之间的功能，手头收尾已完成，已知显示缺口如实保留。后续集中处理华为与 OPPO 的完整迁入迁出，时间和范围见 [当前收尾范围](REMAINING-WORK.md)。

以下 BD 及更早记录为历史事实；“尚未构建”等状态只对应当时批次。

2026-09-07 BD 交付构建：949 项完整测试、前端构建、PyInstaller/NSIS 通过；便携 ZIP 2,349 个文件逐一校验一致。本机 Windows 11 中文路径安装、原生资源、WebView2 桥接、同版本升级、卸载、保留用户导出和桌面快捷方式等 9 项检查通过。新包卸载器实际执行成功，未重试旧 BC 被拦截文件、未更改系统策略。安装验收脚本另修复 PowerShell 5.1 读取无 BOM UTF-8 中文路径证据的问题，专项测试通过；该脚本修复不改变二进制。校验值见交付目录 SHA256SUMS.txt。此为本机开发预览验收，不能替代干净系统和完整七平台正式门槛。

BD 后续矩阵：荣耀综合种子 BD5、魅族综合种子 BD6 各一条正文/双图片/分组严格回读通过，各自原有 27 条不变。荣耀→小米 BD7 两条、魅族→小米 BD8 三条（含原始空标题）严格 API 回读通过，目标原有笔记分别 13、15 条不变；五条已知视觉项目已有记录，当前编辑器 ID 的精确绑定和共享源端复读待补验。魅族→vivo BD9、BD10 均在写入前停止，不再启用；后续只读取确认回执绑定的工具生成 ID，全量 lab fetch 入口已拒绝执行。官方默认分组 ID `0` 的误拒已修复，新批次另行核对。

华为 BD2 精确只读核对通过正文、分组和两张图片原件；全账号 18 条后续读取在第 16 条后返回 401，完整源端基线仍待确认，不重建已确认笔记。源码另修复 Markdown 空白边界的加粗、斜体和删除线，19 项独立合成渲染回归通过；此修复尚未进入上述 BD 二进制。历史私人笔记导出不再作为新验收输入。

2026-09-07 BD 当前检查点：源码已启用五目标受限开发预览迁入（小米、vivo、WPS、荣耀、魅族；PNG/JPEG，小米单张最多 4 MiB 且强制当前账号非加密检查），华为和 OPPO 未开放。新增读取一次的迁移选择、兼容性报告、旧版本未知回执保护及只读记录。专项测试、独立源码前端构建与隐私扫描通过；完整新包尚待统一构建，旧 BC 二进制不包含本轮变化。BC 639 项构建测试、5/9 安装检查通过；卸载程序被系统策略阻止，未更改策略。正式验收门槛保持未通过。

BD 云端检查点：WPS→小米固定两条的严格回读、源端 29 条/42 处附件引用和两条官网基础样式/四张图片通过；荣耀→vivo 四条官网及源端 27 条/32 处附件引用通过。华为直接综合种子 BD2 已确认创建但完整回读待会话问题修复，不重建。677 条缓存的 56 组导出转换通过，BD2 未回读内容不在范围。后续只迁移固定直接合成来源，已通过条目不重复。

以下为历史检查点，旧包能力、哈希与通过数量仅对应各自批次。

2026-09-07 AY：新增独立生成的 WAV/VP8 WebM 与 PNG 离线验收。四格式×两模式共 8 项 ZIP 搬移后正文、时间和附件可达性检查通过；HTML 单条与合并文件、搜索翻页后共 8 次音视频播放正常结束，图片解码通过，外网请求和 JS 错误均为 0。仅覆盖自制样例，不代替厂商附件或其他编码验收。旧阅读器检查脚本改为仅在实际观测到音频/视频时写入相应通过项，缺少样本会明确列出。本轮无产品代码修改、无云写入，运行包仍为 AX（526 项完整测试、9 项安装检查）。

2026-09-07 AX 交付：修复 DOCX 嵌套列表和独立列表编号串联；七平台当前 658 条缓存笔记的 14 项 DOCX 回归通过。526 项完整构建测试（包含真实进程中断测试）及本机 9 项安装检查通过。无云端写入，完整迁移矩阵、厂商原生显示及干净系统验收仍未完成。安装包 SHA256：`17408127a07f33413ce6a962808e914a618c68f009fc1b474c0a288efb53199e`。

2026-09-07 AX：修复 DOCX 嵌套列表、段落分隔列表及跨笔记列表的编号串联。每层独立编号实例，保留已有缩进并复用统一编号规则。合并和多文件模式先复现失败，修复后 76 项导出检查及 Ruff 通过；当前缓存回归与新包验收进行中，无云端写入。

2026-09-07 AW 交付：修复 DOCX 链接文字的组合样式与行内布局；七平台当前 658 条缓存笔记的 14 项 DOCX 回归通过。524 项完整构建测试（包含真实进程中断测试）及本机 9 项安装检查通过。无云端写入，完整迁移矩阵、厂商原生显示及干净系统验收仍未完成。安装包 SHA256：`a117c677a0f4fb55ca19c0b64a284aca28d0a5dfb9428dab3a759bbd3822f0fb`。

2026-09-07 AW：修复 DOCX 超链接文字丢失组合样式的问题；链接与普通正文共享加粗、斜体、下划线、删除线、高亮和代码字体处理，换行及制表符使用标准 Word 元素。合并和多文件回归先复现失败，修复后导出模块 74 项检查通过，Ruff 通过。真实缓存 DOCX 回归及新安装包验收进行中；无云端写入，完整矩阵仍未通过。

2026-09-07 AV：新增三个真实子进程强制终止验收并通过。分别在本地模拟目标已落盘但未回执、已确认首条后，以及缓存替换事务中途终止进程；重开 SQLite 完整性通过，两账号缓存保持，未确认写入两次恢复均不重发，已确认项跳过且剩余项只创建一次，笔记及上传资源标识保留。此为本地故障验收，不替代厂商云端或断电实测。新增测试 Ruff 通过；运行包仍为 AU（519 项完整构建测试、9 项本机安装检查），本轮仅源码新增三个已通过测试，无产品代码修改。荣耀 login_incomplete、魅族 session_expired；未执行云写入，OPPO 暂停。

2026-09-07 AU 交付：登录提示依据原包静态布局调整，保留独立说明和图标。七平台提示与前端操作检查、519 项构建测试、本机 9 项安装检查通过。原包登录被会员弹窗拦截，完整弹窗截图还原、复杂迁移矩阵和干净系统验收仍未通过。无云端写入，OPPO 暂停。安装包 SHA256：`3af717da8e285b3fd268ed7041446f1794c973f6183564184fc2c8ecbdd2d9fe`。

2026-09-07 AU：登录提示按原包静态组件定义改为 768px 面板、居中 64px 图标和 15px 标题，调整说明区域及底部按钮比例；说明和图标独立制作。七个平台提示布局、取消不启动登录、登录后检查账号、导出与迁移共享会话等隔离前端检查通过。84 个初始卡片控件与原包偏差仍不超过 2 CSSpx。原页面点击登录被会员弹窗拦截，因此未声称登录弹窗截图对照通过；其他状态和 Windows DPI 仍待完整验收。无云端写入。

2026-09-07 AS：完成发布前证据审查并新增 RELEASE-READINESS.md。六个非 OPPO 平台的 30 个方向中，17 个存在综合批次记录（含失败/中断，并非通过），13 个尚无综合批次记录。补齐荣耀当前 23 条、魅族 23 条、华为 13 条缓存的 24 种本地导出组合，全部通过；七平台当前缓存共 658 条的导出证据均已更新。acceptance.json 开发索引从旧 368 项测试/588 条缓存更新到当前 519 项测试及实际缓存数，并补入批次和官网证据路径；所有正式状态保持原样，检查仍为 112 项未通过。此次无云端读取/写入，OPPO 暂停未改变；已发出荣耀、魅族、华为会话恢复请求。预览程序仍为 AR 已验证安装包，本轮只更新文档/证据索引和源码包。

2026-09-07 AR 交付：519 项完整构建测试和本机 9 项安装检查通过。预览包包含回执优先的恢复修复：已确认项跳过旧附件验证，未知写入优先待核对，待迁入项仍完整预检查。无真实云端写入或历史回执修改；正式迁移矩阵未通过，OPPO 暂停。安装包 SHA256：`0417ca96b15a380ce6525cec891ebbbe37a9ab5b72f8121408abf6d0f60f6fbf`。

2026-09-07 AR：迁移恢复先核对持久化回执，再验证待迁入内容。已确认成功的笔记不再因旧本地附件缺失而阻塞剩余条目；sending/uncertain 回执在验证和任何新写入前进入待核对，未成功条目仍须通过完整附件检查。使用真实 WPS 预检查器和本地回执复现两项失败，修复后 56 项可靠性测试及相关 Ruff 通过。测试没有发出云端请求；此前真实中断回执未重发或改写。完整构建与安装检查待本轮后续记录，正式矩阵和 OPPO 暂停保持不变。

2026-09-07 AQ：vivo T1 既有表格样例完成官网只读核对。原始回执、当前缓存与历史指纹一致；指定分组和唯一身份正确，三行三列每格文字、空单元格坐标、无合并单元格、两张图片及完整非空白正文顺序通过。检查入口仅额外接受固定 T1 已消费任务，错误账号/平台/索引三个范围检查均拒绝；表格结构不符时不报通过。此次云端创建/更新/上传为 0。荣耀、魅族、华为当前独立登录检查仍分别为 login_incomplete、session_expired、login_incomplete，相关写入未启动。此证据不扩大为合并表格、手机同步或完整迁移方向通过。程序主体仍为 AL 的 516 项构建测试、本机 9 项安装检查预览包；本轮更新验收脚本和源码包，OPPO 暂停，正式发布未通过。

2026-09-07 AO/AP：魅族→WPS Y2、荣耀→WPS Z2 各四条已迁入样例完成官网只读核对。八条均从指定分组按确认回执唯一身份打开，完整非空白正文顺序、十二张图片、基础样式、待办状态及有序列表通过；两条综合样例的组合样式与原降级报告一致。源端空标题在目标已生成首行标题，历史链接/删除线/高亮等损失仍保留，不追溯改称原样还原。此次无云端创建、更新或上传。手机同步、完整空白排版及各方向未覆盖内容仍待验收。程序仍为 AL 的 516 项构建测试、本机 9 项安装检查预览包，源码包和校验值更新；OPPO 暂停，正式发布未通过。

2026-09-07 AN：小米→vivo AK2 四条已迁入样例补齐官网分组与正文检查，四条均从选中的指定笔记本按确认回执唯一身份打开。完整非空白正文顺序、六张图片、来源已有样式、待办、列表及结构逐项比对通过；综合样例组合样式、链接、二级标题、引用和分隔线保留。未创建、更新或上传云端内容。旧 AK2 基础检查证据保留，不能将此次结果扩大为空白版式、手机同步及完整方向验收。同步更新矩阵主表中的 AM/AL1/AN 已验证项目。程序仍为 AL 的 516 项测试、本机 9 项安装检查预览包，源码与校验值更新；OPPO 暂停，正式发布未通过。 链接默认 CSS 下划线与笔记显式下划线分开核对：前者保留官网外观，后者按 U 元素或内联格式检查；首轮过严比较失败记录保留。

2026-09-07 AM：WPS→vivo AK1 四条已迁入样例完成加强官网检查：先展开笔记本侧栏，再在分组树确认选中状态，按确认回执唯一标识打开；四条完整非空白正文顺序、六张图片、基础样式、待办状态、列表及已有结构通过。此前默认折叠侧栏、未放行指定笔记本读取接口、同名页面标题干扰等定位失败保留。读取接口经官网公开代码核对后仅放行 getNoteForNoteBook，云写入仍阻止，本轮云端新增/更新/上传为 0。旧 AK1 空段落原失败及只读修复核对记录保留；此证据不扩大为空白版式、手机同步或完整方向通过。程序主体仍为 AL 的 516 项测试及本机 9 项安装检查预览包，新增检查脚本与说明已重新打入源码包。OPPO 暂停，正式发布门槛保持未通过。

2026-09-07 AL1：新增一条 vivo→WPS 综合样例完成真实迁入，正文、完整链接地址、两张图片原件与分组回读通过；原有 27 条目标笔记指纹不变，精确新增一条。vivo 源端 556 条及 45 处附件引用复读不变。官网唯一身份、分组、两张图片、基础样式和完整非空白正文顺序通过；网址可见，但首次打开没有可点击链接，“可点击链接”检查失败，原链接样式降级不能标为恢复。官网公开代码显示初始化直接加载内容，后续变化才增加网址识别；应用保持明文地址及明确提示。WPS 当前 28 条的 8/8 导出通过。旧 AK3 链接丢失和本次首次/复查证据均保留。程序仍为 AL 的 516 项构建测试、本机 9 项安装检查预览包；本轮仅增强验收脚本并更新说明，正式发布未通过，OPPO 暂停。

2026-09-07 AL 交付：516 项构建测试、本机 9 项安装检查通过；预览包已包含 WPS 链接明文地址保留修复。新链接云端显示仍待验证，正式发布未通过。安装包 SHA256：`c9f74e47c5089c5ed5269e4e4a4cf3f90089d030f1bc5adf197cc076370e22ee`。

2026-09-07 AL：WPS 命名链接现在保留标签并附完整明文地址，明确报告原链接样式未保留；标签本身为完整网址时不重复追加。官网公开编辑器代码显示链接由正文网址自动识别，保存格式不包含独立标签到地址映射。新增测试要求地址、标签样式及特殊字符保留，不安全地址在任何云请求前停止；矩阵新提示必须对应真实保留的网址，旧批次损失证据保持原样。516 项完整测试和相关 Ruff 通过；云端及官网新行为尚未验收，未将旧 AK3 链接损失记录改为成功。收到华为已登录回复后独立检查仍为 login_incomplete，账号标识计数为 0，云端写入为 0；原 AA2 四条修复核对结果保留。正式发布门槛仍未通过，OPPO 暂停。

2026-09-07 AK3：小米→WPS 四条综合/同名/空标题衍生样例正文、图片、分组回读通过；目标原 23 条指纹不变，仅新增四条；小米源端 11 条及 12 处附件引用不变。WPS 当前 27 条的 8/8 导出通过。四条官网按目标分组和确认回执唯一标识打开，正文非空白字符的完整顺序、六张图片、粗体/斜体/下划线、待办状态、有序列表通过；综合样例降级后的组合样式与报告一致。选择器限定可见导航和笔记项后定位通过，之前失败记录保留。官网图片读取凭据请求经官网代码核对后仅放行读取路径，未执行云端修改。该证据不等同于空白版式、完整格式或手机验收；当前 WPS 超链接只保留文字、地址未迁入，需继续补齐。新增共享范围解析和 WPS 验收脚本，程序主体仍为已验证的 509 项测试构建。

2026-09-07 AK2：小米→vivo 四条综合/同名/空标题衍生样例的正文、图片、分组回读通过；目标完整 556 条中原 552 条指纹不变，精确新增四条。分组重名调整有明确提示，原名称附在正文。小米源端 11 条及 12 处附件引用复读不变。四条官网按确认回执唯一标识逐一打开，六张图片、已有样式、待办完成状态与列表结构通过；综合样例五种样式及组合样式、链接地址、二级标题、引用、分隔线通过。一次编辑区定位超时保留原记录，独立只读复查通过，未重写笔记。当前 vivo 556 条的 8/8 导出通过。限定官网检查不能扩大为分组界面、手机同步或完整方向验收。小米→WPS AK3 四条已准备，未启用；荣耀/魅族/华为待恢复会话，OPPO 暂停。本轮只增加验收脚本和说明，程序仍为已验证的 509 项测试预览构建。

2026-09-07 AK：WPS→vivo 四条综合/同名/空标题衍生样例完成创建，完整目标 552 条与原 548 条基线核对通过，精确新增四条。WPS 源端 23 条/32 处附件引用复读不变。综合样例严格比对发现一个空白段落遗漏，原始云端正文 p/br 结构仍在；修复共享 HTML 解析器，保留连续空白段落且不引入容器排版空白。原云端正文哈希不变，重新解析比对通过；仅修正这条本地缓存，其余 551 条不变，未更新/新建/上传云端内容。原失败证据保留，四条当前缓存严格比对通过。509 项完整测试、相关 Ruff、当前 vivo 552 条的 8/8 导出通过。四条官网按唯一标识的图片/标题检查通过，共六张图片正常；综合样例正文约 1.5 万字已加载，不能扩大为全部样式、分组界面或手机同步通过。荣耀/魅族/华为需恢复会话，OPPO 暂停；AK2 小米→vivo 三条已准备但未启用。

2026-09-07 AJ：修正小米官网重名笔记定位，按确认回执的唯一标识匹配真实笔记组件；WPS→小米 AD5 三条及 vivo→小米 AD1 后两条分别打开验证。八张图片解码与可见性、粗体/斜体/下划线、待办两种状态及有序列表均通过，所有云写入被阻止。证据为私有 xiaomi-AJ-native-verification.json；这仅关闭五条样例的官网基础显示待办，不代表完整方向通过。华为独立会话仍缺少 userId（计数为 0），本次未写入；此前 AA2/AA5 四条确认回执和修复回读记录保留。AD2 首条图片申请仍待核对，AD3/AD4 尚未执行，OPPO 暂停，正式发布门槛不变。

2026-09-07 AI：WPS→小米 AD2 首条在图片申请阶段结果不明，笔记远端标识为 0，未进入分组或笔记创建，完整目标八条不变；原始未知分配记录保留，未重新启用该批次。WPS 图片名无扩展名，现按校验后的图片字节格式补齐，并增加不泄露响应正文的数字错误码诊断；暂不认定这是原失败的唯一原因。独立 AD5 仅执行其余三条，正文、图片、分组与新增数量 API 回读通过，原八条不变，当前小米十一条。WPS 源端 23 条/32 处附件引用复读不变。505 项完整测试、相关 Ruff 和当前小米 8/8 导出通过。按目标 ID 的官网定位未匹配，原 data-id 代码属于待办组件，不能当作笔记选择器，官网显示验收待补。缓存 645 条；首条及其图片申请仍待核对，另三条不得扩大为完整方向通过。AD3/AD4 未执行，安装包仍 AC，OPPO 暂停。

2026-09-07 AH：三条 vivo→小米综合/同名样例在原记录上完成限定正文修复，新建 0、上传 0、更新 3；每次回读均核对其余七条原始响应不变，六处图片原件与来源一致。重新获取小米全部八条后，三条综合样例及 AC6 的正文和图片严格比对通过。首条综合样例官网双图片、删除线、高亮、五种组合样式、二级标题、引用、分隔线和链接卡片标题/网址通过；另两条同名样例为 API 回读通过，尚未分别打开官网验收。链接布局转换必须明确报告，不允许掩盖网址和周围样式丢失；多行或结构性卡片仍待核对。499 项完整测试、相关 Ruff 及小米当前八条缓存的 8/8 导出与 ZIP 检查通过。旧缓存指纹已独立保存，未改写原始迁移和中断证据。后续 AD2/3/4 未执行；安装包仍 AC，正式发布门槛未通过，OPPO 暂停。

2026-09-07 AG：通过官网公开代码及两次被拦截的实际更新请求确认，更新必须保留完整笔记字段。新 AG 限定任务保留图片元数据、分组、状态和时间，仅替换 AC6 正文，服务器返回成功码；正文回读一致，其他七条原始响应不变。AF 结果不明及随后旧内容未变记录均保留，未重新启用原任务。官网双图片、加粗、斜体、下划线、已完成/未完成待办及有序列表结构已通过。下划线由 U 元素的 1px 实线下边框显示，已修正检测器漏判，原测量证据保留。493 项完整测试及相关 Ruff 通过。三条 AD1 综合样例尚待同协议限定修复及官网复验，后续 AD2/3/4 未执行；缓存、AC 安装包及正式发布门槛未更新，OPPO 暂停。

2026-09-07 AF：小米现有 8 条笔记完成只读原始响应摘要记录；AD1 三条与 AC6 一条共四条样例均有确认回执，各两张图片与来源原件一致。一次 AC6 限定正文更新携带原版本标记，返回结果不明；未重试、未创建或上传。随后独立只读核对确认原 AC6 整条响应及其余七条均未改变，新正文不在目标端。原中断结果单独保留，不能据此改称修复成功。下一步核对官网更新请求完整字段及响应，原任务不可重新启用。489 项完整测试通过，正文编码仍通过离线官网解析器交叉检查；新增修复测试覆盖版本变化停止、冲突不重试及只读核对。AD1 修复和 AD2/3/4 批次均未执行，缓存与 AC 安装包未更新，OPPO 暂停，正式发布门槛保持未通过。

2026-09-07 AE：小米原生标题、分级列表、引用和分隔线编解码已实现；输出使用 new-format 标记及实际换行符。独立读取器按厂商专用标记区分任务、列表和普通符号，并按原生规则把链接提取为段落后的卡片。486 项完整测试及相关 Ruff 通过。使用已采集官网公开模块在隔离离线环境交叉验证实际编码，确认标题、列表缩进、引用、分隔线、勾选、跨行样式、空段落和链接卡片，云端请求为 0；原厂模块仅作私有测试参照，不进入应用或公开源码包。尚未完成云端旧样例限定修复及官网显示复验，多行引用和卡片布局需继续核对；小米链接矩阵比较仍拒绝未验证的布局。AD2/3/4 未执行，安装包仍为 AC，完整发布门槛未通过，OPPO 暂停。

2026-09-07 AD 原生标记修复：小米写入已改用 b/i/u/delete/background 专用标签，待办完成值改为 checked="true"；读取不再把官方不支持的 strong/em/s/mark/code 等通用标签当作样式成功。行内代码字体降级明确报告；链接卡片排版未验证时不判接口比对通过。477 项完整测试和相关 Ruff 通过。vivo 源端 548 条笔记及 33 处附件引用复读不变。华为收到重新登录回复，但本次检查仍为 login_incomplete，未重复写入；AA2 第三条已修复为历史核对结果。小米原生标题/列表/引用及链接卡片、旧样例修复和官网复验仍待完成，AD2/3/4 未执行。修正解析会改变旧错误编码样例的语义指纹，不能据此断言云端源数据变更，也不能覆盖旧基线。当前安装包仍 AC，未包含本次修复；正式发布门槛未通过，OPPO 暂停。

2026-09-07 AD1 检查点：vivo→小米三条综合/同名样例的接口回读通过，目标原有五条不变；官网双图片正常，但删除线、高亮、组合样式未按预期显示。直接检查官网解析器确认它使用 b/i/u/background/delete 等专用标记，复选框 checked 需为 true；亦支持原生有序列表、标题和引用。该证据优先于通用 HTML 自写解析器的回读结果，不把“字段还在”当作实际显示通过。后续 WPS/荣耀/魅族→小米各四条已准备但未执行，先修正文协议。461 项完整测试通过；vivo 548 条源端复读运行中。缓存 642 条，其中 AD1 新增三条的显示验收未完成。当前安装包仍 AC，新增批次工具和提示尚未打包；正式矩阵仍 0/42，OPPO 暂停。

2026-09-07 AC 交付：454 项构建测试、本机 9 项安装检查通过；已包含小米普通非加密图片上传、受限下载跳转和图片后换行解析修复。AC6 双图片、正文/样式、分组、原件回读、官网显示及 8 种导出已核对。安装包 SHA256：`8db83de59b76c1efa24f6213836b9c58fb63b54b08d5b3da0e908bafd7877d2c`。下方“尚未打包”为构建前记录，已由本条更新；正式发布仍未通过。

2026-09-07 AC 检查点：小米 AC6 双图片创建、正文/样式、分组、图片原件回读通过，原有四条笔记指纹不变；小米当前五条缓存的 8/8 导出和 ZIP 通过。原 AC6 比对失败来自图片后结构换行被解析为空段落，已修复并只读复核；未重复创建。AC1–AC4 仅申请后停止，AC5 首图提交后遇到正常下载跳转误报登录失效，修复后原件回读通过，AC6 已去重复用该文件。原始证据保留。454 项完整测试通过。缓存 639 条，已验证测试笔记 80 条、待核对草稿仍 5 条，另有两条历史华为详情记录。官网两张 640×240 图片可见且解码正常；源码尚未打包，当前安装包仍 AA。完整矩阵仍 0/42，OPPO 继续暂停。

2026-09-07 AA 交付：423 项构建测试、本机 9 项安装检查通过；已包含 Cookie 域轮换修复和华为写入中断原因提示。安装包 SHA256：`4cc6e5f0fe0a8f6e3552e5975618445d1a9d0d30390dc82d80e80a1eb1f82f01`。源码与全部交付物校验通过。下方 AA 段中的“尚未打包”为构建前状态，已由本条更新；正式发布仍未通过。

2026-09-07 AA 检查点：魅族→华为四条综合/同名/空标题来源样例已完成 API 回读与源端复读。第三条已有两张图片，仅修复原草稿正文关联（上传 0、新建 0、更新 1），第四条使用独立新批次执行；原始中断证据保留。目标原有 9 条、魅族源端 23 条及 28 个附件引用不变。前三条归入冲突编号分组 M (2)，空标题来源在目标按正文首行生成标题，均明确记录。WPS→荣耀四条 API 回读及源端 23 条/32 个附件引用复读通过。当前缓存 638 条，已验证测试笔记 79 条，待核对草稿仍 5 条，另有两条历史华为详情记录待核对。

423 项完整本地测试通过；新增 Cookie 域前导点保留修复，避免服务端轮换后同域旧值重复保留，但未证实这是华为会话反复失效的原因。荣耀→华为 AA3 在登录检查阶段停止，无写入，不能重发该批次标识；后续华为批次未执行。小米桌面编辑器的官方图片上传协议已找到，见 XIAOMI-UPLOAD-PROTOCOL.md；仍未验证真实图片上传。完整方向保持 0/42，OPPO 继续暂停。当前安装包为 Z（413 项构建测试、9 项本机安装检查），上述源码修复尚未打包。

2026-09-06 Z 检查点：413 项构建测试、本机 9 项安装检查通过。华为 W4 第三条在原草稿上补齐一张图片，未新建笔记；恢复后其余 8 条不变，三条正文/双图片/分组及用户官网确认通过；源 vivo 548 条及 33 个附件引用不变。荣耀→WPS Z2 四条 API 与源端 19 条复读通过；荣耀→魅族 Z3 四条 API 通过、目标原有 19 条不变，源端重新登录后 19 条及 20 个附件引用复读不变。WPS→魅族 Z1 首条图片上传不明，保留无图草稿；按图片字节补齐上传文件扩展名后，仅执行未尝试的 Z4 三条，API 与源端 23 条复读通过。所有中断原始证据保留，无自动重试。缓存 630 条，含 71 条已验证测试笔记、5 条待核对草稿；另有两条历史华为详情记录待核对。完整方向仍 0/42。OPPO 按用户要求暂缓；后续除必要登录和用户独有操作外自行验收。安装包 SHA256：`b27615792ce3d4e3c65338b026d388a9446cbc45558c1aed48dfc5752ed51e11`。

2026-09-06 W/Y 检查点：vivo→魅族/WPS/荣耀综合与同名三条样例 API、用户官网确认通过；vivo 源端统一复读运行中。魅族→荣耀/WPS 四条（含真实空标题）API 与源端 15 条复读通过，目标把空标题按正文首行生成标题。华为 W4 两条内容/双图核对通过，第三条仅一图且 uncertain，未重发；旧版工具丢失的迁入前指纹不补称通过，后续批次已改为写前落盘。390 项完整测试加 28 项定向通过。缓存 618 条；正式完整矩阵仍 0/42，详细范围见 MIGRATION-MATRIX.md。安装包仍 S2，魅族分隔线提示修复尚未构建。

2026-09-06 U/V 检查点：376 项完整测试通过。荣耀→魅族三条样例 API、源端复读与用户官网显示均通过；荣耀→WPS 三条（2 新增、1 跳过）按明确降级复核通过。vivo 综合 V1 及同名 V2/V3 云端回读通过，548 条全量读取待执行。当前缓存 597 条，含 39 条已验证测试笔记；完整矩阵仍 0/42。最新范围与证据见 MIGRATION-MATRIX.md。源码归档尚未刷新，安装包仍为 S2。

2026-09-06 T1 检查点：vivo 三行三列表格、空单元格、中文、Emoji、特殊字符与双图片混排已通过真实云端回读，复用 M 分组且未创建新组。新增后 vivo 545 条；本次 369 项完整自动测试通过。545 条全量复读仍在进行，原有 544 条内容是否不变及官网表格显示尚待核对，不能记为完整方向通过。任务已消费，不得重发 T1。安装包仍为已验收 S2；本次仅测试工具与记录变更，源码归档待刷新。OPPO 继续暂停。

# 实施检查点

S2 交付完成：368 项构建测试及本机 9 项安装检查通过，安装包已包含 HTTP403 分类修复。安装包 SHA256：`92c4af1e80b13d686d5e99df2196948d15b65eecfffa289e84a0a4eb6c08ed41`。预览说明已改为当前状态，旧版说明存入私人检查点；正式门槛和历史失败记录未变。

2026-09-06 S2：删除线、高亮、链接、组合样式、二级标题与双图片样例云端回读通过，原有 11 条笔记指纹未变，新增后荣耀 12 条。官网样式及双图片显示通过。首次检查仅检测 text-decoration 而误判下划线；官方 CSS 使用可见实线下边框，按该规则复查通过，未修改笔记或生产序列化。S1 的 HTTP403 失败和检查器误判证据均保留，不推断厂商拒绝的确切原因。368 项完整测试、荣耀 8/8 新缓存导出通过；当前缓存 588 条，30 条已验证测试笔记。正在构建包含 403 分类修复的新预览包，N2 仍为上一版已验收安装包。

2026-09-06 S1 检查点：补充删除线、高亮、超链接、组合样式、二级标题和普通 HTML 标签文字的云端样例。本地荣耀往返测试通过；真实首次保存返回 HTTP 403，0 成功创建、0 图片上传。随后账号检查通过，11 条全量复读指纹不变；不能把此次拒绝归为登录失效。公共请求层现在将 403 单独报告为 request_forbidden，各平台读取保留遇拒绝立即停止的行为，未增加自动重试。367 项完整测试、62 项定向测试和相关 Ruff 通过。S1 原始失败及后续核对证据保留，禁止自动重发；富文本真实验收尚未通过。生产代码变更尚未并入 N2 安装包，OPPO 云端操作继续暂停。

2026-09-06 R2 检查点：荣耀云端 N2 双图片笔记真实迁入 WPS，源端 11 条复读指纹未变，目标原有 9 条指纹未变、新增后 10 条。正文逐行样式、图片原件、分组及官网显示均通过；官网从 N 分组打开迁入笔记，双图均 640×240。WPS 段内换行转独立段落已报告，并通过逐行字序和样式复查，原 needs_review 证据保留，未重复写入。362 项完整测试、13 项定向测试和 WPS 新缓存 8/8 导出通过。当前缓存 587 条，含 29 条已验证测试笔记；完整复杂迁移矩阵仍 0/42。当前应用安装包仍为 N2，R2 仅更新测试工具及证据文档，正式发布仍阻断。

N2 交付检查点：360 项完整构建测试、本机中文路径安装/WebView2/覆盖安装/卸载保留导出等 9 项检查通过。安装包 SHA256：`35c78b9f3fb9243de985419dfcd4c8d13c6ed0d82485ab52394f8714252bbfd7`。最新 586 条缓存的 56/56 转换与 ZIP 通过（`.private/live-exports/20260906-214128-4e30bb/evidence.json`）。Q1/Q2/R1 更新均已并入预览包；内置兼容说明的测试数量为构建前 358 项加 11 项定向检查，最新构建结果以此记录为准。用户重登后荣耀、魅族、华为独立检查已通过；OPPO 继续暂停，正式门槛仍 112 项未通过。

2026-09-06 N2 最新检查点：荣耀 N1 建组、N2 复用（新增建组请求 0）、双图片与正文回读通过；分别核对原有 9 条、10 条笔记指纹未变。官网从 N 分组打开 N2，两张图片均解码为 640×240。WPS→vivo R1 全量复读完成：544 条缺失/额外/变化均为 0，25 个附件引用校验通过；源 WPS 9 条指纹未变。当前缓存 586 条，28 条已验证测试笔记；正式迁移矩阵仍 0/42。用户重新登录除 OPPO 外所有厂商，荣耀已实测有效；OPPO 继续暂停。现有 P2 安装包待统一更新 Q1/Q2/R1 及官网验证脚本。

2026-09-06 R1：真实 WPS 云端 J2 双图片笔记已通过源账号/创建回执/快照及图片校验后迁入 vivo，目标新增 1 条、建组 1 次，图片原件和分组回读通过。首次严格片段比较为 needs_review：同样式时间文本在 WPS 被分片、vivo 合并；补充仅合并相邻同样式片段的等价比较后正文与样式通过，原始未通过记录保留，没有重复写入。WPS 源端 9 条完整复读指纹未变。vivo 544 条全量复读正在运行，以验证原有 543 条和新增笔记；官网显示未验收。358 项完整测试通过。该样例不是完整方向验收，正式矩阵仍 0/42。Q1/Q2/R1 源码尚未并入 P2 安装包。

2026-09-06 Q2 检查点：发布校验改为验证 56 个唯一导出组合、42 个唯一方向、6 个系统/缩放组合和 8 个界面状态；通过项必须关联项目内实际存在的 JSON 正式报告，报告中有且仅有一个同范围 passed 检查。缺失文件、目录越界、开发证据、错误方向和重复条目均阻断。11 项针对性测试、349 项完整测试及相关 Ruff 通过。当前仍有 112 项未通过，未修改验收通过标记。小米图片申请上传/KSS/提交路径已从原包静态定位，但尚缺官方契约与实测，不启用上传。Q1/Q2 源码尚未并入 P2 交付包。

2026-09-06 Q1 读取检查点：小米读取增加账号归属、起止文件夹目录一致性和详情标识检查；缺失文件夹保留来源标识并明确报告不完整。账号或目录变化不提交快照，原缓存保留。17 项针对性测试、338 项完整测试及相关 Ruff 通过。真实只读复测 4/4 条成功、0 项提示，证据 `.private/evidence/xiaomi-Q1-readonly.json`；未进行云端写入，本次未另作读取前后内容指纹对比。当前交付物仍为 P2（333 项构建测试），Q1 源码尚未打包。现有官方小米脚本仅定位到图片下载，图片上传契约仍未确认。

P2 交付检查点：当前源码完整构建 333 项测试通过，本机中文路径安装、WebView2、覆盖安装、卸载保留导出等 9 项通过。安装包 SHA256：`aac0933de87423012c1513b7695846ca0d26dd9b446b5b5af60663184b95dbf8`。安装包已包含 vivo、荣耀候选及小米分组实现；内置兼容说明记录的是构建前 332 项完整测试加 12 项小米定向测试，以本记录的构建结果为准。小米新增后的 8/8 转换与 ZIP 通过，证据 `.private/live-exports/20260906-211651-b85be8/evidence.json`；其余平台沿用 M2 导出证据。发布检查仍有 112 项缺少真实通过证据，正式发布保持阻断。

2026-09-06 P2 检查点：小米 P1 新建文件夹与文字笔记、P2 复用文件夹均真实回读通过；P2 建组请求为 0，分别核对原有 2 条、3 条笔记指纹未变。来源时间、常见样式与待办保留，有序列表转换为项目列表并报告降级。332 项完整测试及随后新增接线后的 12 项小米测试通过，相关生产代码和测试 Ruff 通过；诊断脚本单独检查仍有 11 项既有导入排序问题。当前缓存 583 条，含 25 条已验证测试笔记。魅族 L1/L2 分组与双图片用户确认已记录。小米官网显示尚待核对，图片上传未完成；荣耀登录和 vivo 人工显示仍待回复，OPPO 暂缓，正式迁移方向仍为 0/42。当前安装包为 L2，后续源码准备重新构建。

2026-09-06 N1 候选检查点：荣耀分组新建、持久化复用及笔记归组已实现，覆盖来源/目标账号隔离、同名目录隔离、未知写入阻断、目录删除或类型变化及名称长度。327 项完整本地测试通过，相关代码与测试 Ruff 通过。真实检查在登录阶段返回 `login_incomplete`，尚未开始 N1 写入，等待荣耀会话恢复后先读取目录结构再实测。vivo M1/M2 人工显示确认仍待回复；OPPO 继续暂停。当前安装包仍为 L2，vivo/荣耀后续源码未重新打包。

2026-09-06 M2 全量复读检查点：vivo 543/543 条重新读取完成，与 M2 前保存的逐条指纹比较为缺失 0、额外 0、变化 0；23 个附件引用的本地文件大小和 SHA256 全部一致。543 条已有 source_warning 保留，任务状态为 partial，不掩盖格式兼容提示。证据 `.private/evidence/vivo-M2-full-refetch.json`。官网 M1/M2 分组显示的人工确认仍待回复。荣耀分组的真实接口映射已从官方代码确认，下一步实现。

2026-09-06 M2 检查点：vivo M1 创建分组与双图片笔记，M2 复用同一分组（新增分组请求 0）；两条的图片原件、正文样式及分组归属回读均通过。319 项完整本地测试通过。当前缓存 581 条，含 23 条已验证测试笔记。vivo 原有 541 条内容尚待本轮全量复读比较；官网分组显示正在检查，不提前计为通过。当前安装包仍为已验收的 L2，vivo 分组源码变更尚未打包。OPPO 继续暂停。

L2 交付包检查点：新版构建通过 312 项测试，本机中文路径安装、WebView2、覆盖安装、卸载保留导出等 9 项通过；安装包 SHA256 为 `5dc9d193a008b190a67198957f1007905ff0f82e5a4c0cc13a30b50c4caaacb3`。源码包补充用户刚完成的魅族人工显示确认；安装包内兼容说明生成早于该确认，仍显示当时的“官网待核对”，以本文件和 TESTING 的后续证据为准。该预览版本不具备正式发布条件。

L2 人工显示验收补充：用户在已登录的魅族官网确认“笔记互迁分组验收 L”中 L1、L2 两条笔记均正常，L2 双图片正常显示。API 与人工显示证据均通过；独立浏览器导航超时记录保留，未改写为自动验证通过。

2026-09-06 L2 最新检查点：魅族 L1 分组创建、L2 复用及两条双图片笔记归组实测通过，L2 新增分组请求 0；两次新增分别核对原有 6 条、7 条笔记指纹不变，图片原件、正文样式及分组名称/标识正确。312 项完整本地测试通过。现有缓存 579 条，已验证测试笔记 21 条。官网分组显示正在核对；华为 K2 和魅族 L2 的更新准备统一构建。OPPO 仍暂停，正式迁移矩阵仍未通过。

2026-09-06 L1 候选检查点：魅族来源分组读取和目标建组、持久化复用已实现；读取时校验账号和分组目录，缺失分组明确报告，避免静默丢失归属。新增同名隔离、未知结果阻断、已删除映射、16 UTF-16 单位名称限制及创建接线验证。312 项完整本地测试通过，相关生产代码与测试 Ruff 通过。魅族实测前会话检查返回 `session_expired`，本轮未开始魅族写入；等待用户恢复会话后先检查真实目录结构，再执行单条合成测试。OPPO 仍暂停。现有安装包为 J2；华为 K2 与魅族 L1 源码变化尚未打包。

2026-09-06 K2 最新检查点：华为分组创建及只读回执核对通过，K2 复用该分组（新增分组请求 0），双图片创建、归组、原件字节及正文样式回读通过，原有 5 条笔记指纹不变。官网从该分组打开 K2，两张图片均解码为 640×240；滚动并等待异步图片加载后验证通过。列表转换为编号段落的降级保留报告。305 项完整本地测试、577 条缓存的 56 组导出和 ZIP 检查通过。19 条已验证测试笔记，K1 仅建组未创建笔记，历史中断证据保留。当前安装包仍为 J2，华为分组变更待下一次构建；42 个完整迁移方向、其余目标分组、小米图片和干净系统验收仍未完成。OPPO 继续暂停。

2026-09-06 K1 候选检查点：华为目标分组映射已实现，涵盖重名隔离、跨账号隔离、持久化复用、未知写入禁止重试及失效映射阻断。304 项完整本地测试通过。实测前账号检查返回 `login_incomplete`，未开始分组或笔记写入，等待用户恢复华为会话。OPPO 继续按用户要求暂停。当前安装包仍为下述 J2 验证版本；K1 源码变更尚未打包，不算真实迁移验收通过。

2026-09-06 J1/J2 最新检查点：WPS 分组创建、两条双图片笔记归组和分组复用实测通过，J2 新增分组请求为 0；官网已从该分组进入 J2 并显示两张图片。两次新增分别保留原有 7 条、8 条笔记指纹不变。当前缓存 576 条（554 条原始、18 条已验证测试、4 条荣耀待核对草稿），另有 H2/H3 两条华为历史详情记录待核对。295 项完整本地测试通过，最新 56 组导出与 ZIP 通过。OPPO 按用户要求因环境风险暂停，未再发请求；其他平台分组、小米图片及完整 42 方向仍待完成。新版构建通过 295 项测试，本机中文路径安装、WebView2 启动、覆盖安装及卸载保留导出等 9 项检查通过；安装包 SHA256：`87c3a9a3f850e15593387c632f05f279cdf5aa27086ef4b43dde03cb1f4fc585`。

证据：`.private/evidence/fixture-20260906-wps-group-J1.json`、`fixture-20260906-wps-group-J2.json`、`wps-image-browser.json`；导出 `.private/live-exports/20260906-200912-a32736/evidence.json`。以下保留历史记录。

2026-09-06 I1 检查点：OPPO 目标分组候选实现已加入，288 项完整本地测试通过。映射按来源平台、来源账号、来源分组及目标账号隔离；重名时创建编号分组，已确认映射可复用，目标分组缺失或建组结果不明时停止。分组名称按官网 50 个 UTF-16 单元及字符限制处理。I1 真实测试在账号检查阶段停止，数据库无笔记或分组写入意图；尚未验证真实建组，不能算迁移矩阵通过。用户反馈官网提示环境风险，已要求暂停 OPPO、先推进其他平台；本轮无云端新增。新版已通过 288 项构建测试及本机 9 项安装验收，安装包 SHA256 `06ea740c8197ac978b7471da6ca205a45a6567b53f3372eae8e7c3a874224ed4`。

2026-09-06 H4/H5 最新检查点：七个平台已读取 554 条原始笔记，七平台基础创建和回读均通过。除小米外，六个平台已有真实双图片上传及原件回读证据；华为 H5 目录、正文、双图片字节及官网显示通过，有序列表转编号段落的降级已报告。WPS 长正文加双图片另行通过。当前缓存 574 条，含 16 条已验证测试笔记和 4 条待核对荣耀草稿；另有 2 条历史华为图片测试记录仅详情可读，仍保留待核对。281 项本地测试通过。官方网页双图片显示已自动验证 vivo、荣耀、OPPO、WPS、华为；魅族由用户亲自确认正常。小米图片上传、目标分组和完整 42 方向仍未完成，生产迁移继续关闭。

官网 H4 新建后再次保存的真实请求证实更新时正文与请求使用同一云端标识，已修正独立实现；读取时显式标题从 data5 提取，避免混入目录正文摘要。H5 新增前 4 条笔记指纹不变，2 张原图和官网解码通过。证据 `.private/evidence/huawei-native-H4.json`、`.private/evidence/fixture-20260906-huawei-image-H5.json`、`.private/evidence/huawei-image-browser.json`。H5 新版已通过 281 项构建测试、本机 9 项安装验收及最新 574 条缓存的 56 组转换与 ZIP 检查；离线阅读 17 图片和 1 音频通过。安装包 SHA256 `4502f3273442fd7be10520877a6711f9bf117b89d2fe63e169d4168dd49b2a0e`。导出证据 `.private/live-exports/20260906-194400-693780/evidence.json`。以下保留历史记录。

2026-09-06 华为只读定位检查点：官方同步查询返回成功，H3 同步后仍不在普通目录；两份附件和详情存在。正常可见笔记也存在正文标识与云端标识不同的情况，尚无证据支持仅修改标识即可修复。已增加脱敏结构对照和官方页面控件检查脚本，本轮未发出云端写入。证据 `.private/evidence/huawei-H3-sync-review.json`。桌面代码仍为 279 项测试及 9 项安装检查通过版本，正式发布未通过。

2026-09-06 用户核对更新：华为 H3 在官方目录找不到；重新登录后独立只读检查仍为目录 0 条匹配、详情可读且含 2 个附件。魅族 E1 两张图片由用户亲自确认显示正常；独立浏览器导航超时，保留为不同来源的证据。新增华为图片保存后的目录复查：缺失时保留待核对，禁止错误确认或盲目重试。279 项完整构建测试、代码检查与本机 9 项安装验收通过；更新安装包 SHA256 `82bef1cb966e7c0247cb1d064bce2e8a987dd4bd88b11fd06c4ebcfef90e5db4`。目录异常原因仍待定位，正式发布未通过。

2026-09-06 F2 最新检查点：七个平台已读取 554 条原始笔记，七平台基础创建和回读均通过。vivo、荣耀、魅族、OPPO、WPS 各有真实双图片上传及原件回读证据，WPS 长正文加双图片另行通过。当前缓存 572 条，含 14 条接口回读已验证测试笔记和 4 条保留待核对的荣耀草稿；另有 2 条华为图片测试记录可查询详情但未列入普通目录，不能计为完整迁移通过。278 项本地测试、56 组缓存转换及 ZIP 检查通过。官方网页图片显示已验证 vivo、荣耀、OPPO、WPS；魅族会话过期，页面显示待确认。OPPO F2 修正原生图片引用；F1 的接口回读证据不能代表页面正常。小米图片上传、华为目录异常、目标分组和完整 42 方向仍未完成，生产迁移继续关闭。

最新导出证据：`.private/live-exports/20260906-112452-fd1f64/evidence.json`。F2 安装包已完成重建，278 项构建测试及本机 9 项安装检查通过；安装包 SHA256 `0fde97e56eb99bcc0799db3bc09a897b38b9710b114e97b938f42c3e8fbdf4f1`。干净系统验收仍缺失。以下为历史记录，不代表最新状态。

当前状态：开发中，未完成真实账号验收，不得发布正式 v1.0.0。

2026-09-06 H3 工作区状态：277 项测试通过。华为 CSRF 响应头轮换、游标保留、正文内部标识隔离及图片下载/上传候选实现已加入。H2/H3 各两张图片上传与原件校验通过，后续补全文字请求成功，但普通目录未列出测试条目，不能计为完整迁移通过；已请求用户核对 H3 官方页面。H1 未确认创建，保留不确定回执。H2/H3 保留待核对；原有 3 条华为笔记导出内容与 G2 前完全一致。当前缓存仍 571 条，另有 2 条可直接查询但不在普通目录的华为测试记录。正式门槛仍未通过。

2026-09-06 H1 进行中：华为配置请求使用当前官网的追踪标识格式后返回 code 0，已确认 dataVersion 2.0。新版附件候选实现接入，275 项测试与 Ruff 通过，H1 双图片真实写入及回读正在进行，尚未计为通过。安装包仍是 G2 的 265 项测试版本，尚不含华为图片。

2026-09-06 G2 打包检查点：265 项构建测试及本机 9 项安装检查通过，新安装包 SHA256 `e76713a84f9628d52787c1c87c2d5a76755c8daeb222416a4c6ddafd307e95fc`。已包含五平台图片及 WPS 长正文。571 条缓存的 56 组转换与 ZIP 通过；正式门槛仍有 112 项未通过。用户重新登录后华为基础检查通过，图片配置接口返回 -1，正在核对请求契约，未开始华为图片写入。

2026-09-06 G1/G2 工作区检查点：WPS 双图片创建、字节校验及完整回读通过，原有 5 条笔记未变化，新增后的 8 组导出通过。累计缓存 570 条，其中原始 554、已验证测试 12、部分草稿 4。新增 WPS 长正文对象存储支持，265 项本地测试通过，G2 真实长正文回读已通过，既有 6 条笔记指纹不变；新增后缓存 571 条，56/56 转换与 ZIP 复查通过。当前安装包尚不包含荣耀、魅族、OPPO、WPS 新图片实现及 boto3 依赖。魅族、OPPO、WPS 的独立浏览器图片显示尚未通过；华为等待重新登录。

2026-09-06 E1/F1 工作区检查点：魅族 5→6、OPPO 2→3，两组 PNG/JPEG 双图片创建及完整回读通过，既有笔记指纹均不变。当前缓存 569 条：原始 554、已验证测试笔记 11、部分草稿 4。253 项本地测试通过，三个新图片适配仍未打包；生产写入继续关闭。荣耀、魅族新增图片后的各 8 组转换通过。魅族独立浏览器未找到测试条目，已请用户在现有窗口确认；OPPO 页面检查进行中。用户可提供本机电脑，尚无干净系统及手机测试环境。

2026-09-06 D5 工作区检查点：荣耀 PNG/JPEG 双图片新建、完整回读及官方页面解码均通过。荣耀数量 8→9，既有 8 条指纹未变化；两份原图 SHA256/大小、正文位置、样式和待办状态匹配。此前 D1–D4 的 4 条测试草稿及待核对资源记录保留，不自动重试。当前缓存 567 条，其中原始笔记 554、已验证测试笔记 9、部分测试草稿 4。227 项本地测试通过；当前安装包仍对应 212 项测试，不包含本次荣耀图片实现。证据 .private/evidence/fixture-20260906-honor-image-D5.json 和 honor-image-browser.json。

当前交付：更新内置指南与兼容性矩阵后的 C2 安装包已再次通过开发机 9 项安装检查，SHA256 为 `88dbca5fb507b6fe32279fd64b5310f47dee26d6065b5abb1f8a4e9e57edb8ca`。C1 与更早的安装证据保存在私人检查点目录；正式验收仍有 112 项未通过。已询问用户可用安卓手机的品牌与系统版本，以确定后续手机端同步和兼容性实测范围。

C1 完整复查已完成：vivo 全部 541 条与复读前缓存指纹一致，其中 539 条原始笔记未变化；19 个附件大小及 SHA256 全部匹配。快照完整性为 true，543 条排版提示仍保留，任务状态为部分完成而不再报告缺失内容。证据 `.private/evidence/vivo-refetch-C1.json`。当前 562 条缓存的 56/56 转换与 ZIP 验证通过，证据 `.private/live-exports/20260906-091542-ce89a2/evidence.json`；新离线 HTML 的 541 条目录、17 图片和 1 音频检查通过，外部请求及脚本错误均为 0。

2026-09-06 C1 实测：七平台登录检查全部通过。华为、荣耀、魅族分别新增一条明确标记的测试笔记并回读通过，数量为 2→3、3→4、4→5，原有笔记指纹均不变。华为列表转换为普通编号段落并报告降级；本样例文字样式和待办状态保留。至此七平台基础创建已各有真实证据，当前缓存 562 条，其中原始笔记 554 条、测试笔记 8 条；生产迁移仍关闭，不能代替完整迁移矩阵。

C1 构建已修复下述临时目录问题：每次使用经过边界检查的新目录，保留旧失败目录，212 项构建测试通过。PyInstaller、NSIS 及开发机中文路径安装/启动/覆盖安装/卸载保留导出再次通过。新安装包 SHA256 为 `34045f7154be86c31860b571b574f6b57b0191a16acabcdf3e9f8d7f0c4364cf`；仍未完成 Windows 10/11 干净系统验收。

2026-09-06 更新后续接：旧测试浏览器进程已退出，七个官方窗口已重新打开，等待用户完成登录。修复 vivo 读取将默认排版和普通分隔线转换误判为缺失内容的问题；这两种已验证的降级仍保留逐条报告，缺失资源、未知待办状态、专有内容及其他未分类提示仍阻止迁移。新增 7 个回归场景覆盖读取缓存和桌面迁移接口，共 212 项独立本地测试及 Ruff 通过。没有修改既有缓存的完整性标记，也没有新增云端写入或解除生产迁移开关。

本轮打包未完成：TypeScript/Vite 构建通过，但打包脚本重复使用的 `.private/build-test-temp` 在 pytest 清理时发生 `PermissionError / WinError 5`。该次构建中的测试为 47 项通过、165 项临时目录初始化错误；不是 212 项完整通过的打包结果。PyInstaller/NSIS 尚未执行，`dist` 仍是上一轮 205 项测试对应的旧交付物，不包含本轮修复。下一步先使用经过路径边界检查的新测试临时目录恢复构建，再检查安装包；不删除旧目录或改变其权限来掩盖问题。

2026-09-06 08:58 用户要求保存记录并重新调出窗口：当前七个官方窗口继续保留，待用户关闭代理、完成登录并通知继续后，才恢复账号检查及真实测试。运行进程与可续接终端编号记录在 `.private/implementation-checkpoint.json`，后续使用前须验证进程仍存在。凭据没有持久化，因此软件/测试浏览器退出后仍需重新登录。

| 检查点 | 状态 | 证据 |
| --- | --- | --- |
| 还原基线 | 已分析 | REFERENCE.md；原包 SHA256；隔离前端检查 |
| 独立桌面/存储/导出 | 已实现并验证基础通路 | 212 项 Python 测试；真实 WebView2 桥接；8 个界面状态检查 |
| 七平台独立读取 | 七平台均已读取真实笔记 | 小米 1、OPPO 1、vivo 539、华为 2、荣耀 3、魅族 4、WPS 4，共 554 条 |
| 七平台写入 | 七平台基础创建及回读、vivo 双图片通过 | 华为/荣耀/魅族 C1 实测补齐；复杂内容/其余图片/分组待验证，生产迁移入口仍关闭 |
| 56 项导出转换 | 最新 56/56 已通过，正式验收仍未通过 | 554 条原始笔记加 8 条测试笔记；正文、时间、目录、ZIP 及全部已下载附件引用 |
| 参考界面几何 | 初始导出和迁移卡片部分通过 | 84/84 个控件最大偏差 0.5625 CSS 像素；弹窗等其余状态仍待逐项对照 |
| 42 个迁移方向 | 待真实验收 | 未向目标批量复制用户原始笔记 |
| 干净 Windows 10/11 安装 | 待验收 | 不以开发机启动代替 |
| 开发机 Windows 11 安装 | 已通过 | 中文路径安装、冻结窗口桥接、同版本覆盖安装、卸载保留测试导出及快捷方式 |

用户已确认可以配合七个平台的官方登录与二次验证。公开推送及 Release 等待用户的发表指令。

2026-09-05 检查点：保留七个独立官方浏览器会话。调试工作进程通过管道临时使用会话，凭据不进入参数、文件、日志或测试样例。用户完成网络切换及华为/vivo 页面刷新后，七个平台的当前读取协议均已贯通。原软件的授权后台及其加密模块没有参与调用。

实时转换证据：`.private/live-exports/20260905-231450-f54f95/evidence.json`。这是已获取缓存的转换检查；不能替代复杂内容、完整云端范围、真实迁移及干净系统的正式验收。发生过的字段差异（vivo 有效记录标记为 1、华为云标识与客户端标识不同、OPPO 系统分组标识可为空）已修正或纳入合同测试。

2026-09-06 检查点：vivo 全部 539 条已复读，17 个附件（15 图片、1 音频、1 文件）共 20,257,777 字节通过下载、大小与 SHA256 核对，15 图片可解码。8 组含附件导出再次通过，资源在各格式中可到达；证据 `.private/live-exports/20260905-235930-9a0831/evidence.json`。离线浏览器加载全部 15 图片和音频元数据，0 外部请求；搜索与筛选检查记录在 `.private/evidence/reader/evidence.json`。5 条特殊笔记复查后保留 8 个待办状态和 24 个分隔线，仍有 539 条默认排版提示及 4 条装饰线简化提示。

桌面生命周期检查覆盖七平台构造、失败登录的临时会话清理、关闭时阻止会话重新进入、64 位互斥锁及旧程序拒绝降级新版数据库。一次 Windows 原子目录重命名失败在复测时通过，现仅对指定 Windows 错误进行有上限的本地提交重试，不重做导出或重试云端写入。

开发机安装证据 `.private/evidence/installer/evidence.json`。NSIS 的 `/NoShortcut` 不改变现有桌面快捷方式，卸载只删除安装清单中的文件，测试导出完整保留，测试安装注册表记录清理通过。该证据不代替 Windows 10/11 干净环境、跨版本升级及 100%/125%/150% 缩放验收。

2026-09-06 写入检查点：小米、OPPO、WPS 新建后完整复读，既有笔记指纹不变；vivo 新建后按目标标识回读并核对总数，未在这次创建测试中重新读取既有 539 条的全部正文，因此原数据完整性这一项仍未确认。测试内容为中文、英文、Emoji、三种文字样式、已完成/未完成待办及编号列表。小米编号列表转换为普通项目符号，已记录降级；其余三平台本样例无降级。各来源时间附注在正文。

WPS 首次创建请求被拒绝，便签查询返回空；没有将失败记为成功。补齐与官方网页一致的 Origin/Referer 后，用独立测试标识完成个人目录文件、便签信息、加密正文三步创建。多步骤写入前持久保存新标识，中断及未知结果保留该标识并阻止重试。

最新转换证据 `.private/live-exports/20260906-013755-78539c/evidence.json`：558 条缓存、56/56 通过，全部 17 个已下载资源在输出中可到达。离线 HTML 再次通过 540 条 vivo 笔记、15 图片、1 音频和零外部请求检查。四平台创建证据见 `.private/evidence/fixture-20260906-{xiaomi,oppo,vivo}-A1.json` 与 `fixture-20260906-wps-A2.json`。

会话检查中，华为、荣耀身份未确认，魅族会话失效；已请用户在保留的官方窗口重新登录并进入笔记。没有关闭当前浏览器会话。剩余工作为后三平台创建实测、其余六平台图片上传、七平台分组、完整 42 方向及复杂内容/失败矩阵、其余视觉状态和干净系统安装验收。

2026-09-06 图片与候选适配检查点：vivo 的独立上传实现覆盖文件预分配、缩略图、整文件、分片及服务器确认。前两次图片试验没有创建笔记，资源结果未知记录保留；第二次只读查询确认原图/缩略图均返回 HTTP 410。补齐与官方网页一致的 Origin/Referer 后，用新测试标识成功创建含 PNG/JPEG 的笔记，两份原件大小、SHA256、正文位置、样式和待办状态均通过回读。证据 `fixture-20260906-vivo-image-B3.json`。同次任务没有重读全部既有正文，不能记为源数据完整性已验收。

当前 559 条缓存中含 5 条测试笔记。新增图片后的 vivo 8 组转换证据为 `.private/live-exports/20260906-021541-2f7c05/evidence.json`；离线阅读为 541 条、17 图片、1 音频，0 外部请求。图片分片与异常确认仅通过本地合同测试，真实服务此次返回整文件上传。

新增资源写入表记录分配、上传和已关联状态，随笔记确认原子更新，异常或重启不会清除待核对文件标识。荣耀新增 h_n/h-text 原生富文本解析与创建候选；华为候选先读取账号中已存在的格式版本，保留传统正文与富文本双表示。后两者没有进行真实创建，不计入已通过平台。全套 205 项本地测试及 Ruff 通过。

官方 vivo 浏览器检查已通过：选中本次双图片测试笔记后，在其 TinyMCE 正文框架中实际解码两份预览（540×203、640×240），比例匹配；原始字节由前述 API 回读单独核对。证据 `.private/evidence/vivo-image-browser.json`，仅测试正文的截图为 `vivo-fixture-browser.png`。更新后的安装包 SHA256 为 `39706fdfa99afc3a271a3a9b1ee4c1d10e6f995c46281cea5f0d7b53badfc004`，开发机中文路径安装、冻结版桥接、覆盖安装及卸载保留导出再次通过。
2026-09-07 BC 进行中：重启后测试解释器的未签名组件被 Windows 应用程序控制阻止；已改用 Python 官网同版本签名组件，平台后台恢复。小米→荣耀 BC1、→魅族 BC2 各四条的正文、六张图片和分组回读通过，两个目标原有各 23 条不变；一次小米复读核对 11 条笔记及 12 处附件引用不变。小米→华为 BC3 第三条中断，唯一回执与已上传资源保留；第四条未提交，后续只读核对及限定恢复。荣耀→vivo BC4 正在执行。生产侧修正华为首次 CSRF 来源，并让荣耀获取时复用与最新远端 SHA256/大小一致的附件，新上传仍强制回读。构建和官网显示验收继续进行，正式发布未通过。

华为新四条官网检查：本地账号、回执和原件范围通过；实际浏览器等待超时后只结束本次独立检查进程，保留用户登录窗口，未取得显示通过。只读初始化端点与有界等待已修正并通过 9 项定点测试，本轮没有再次云读取；该官网缺口保留。
